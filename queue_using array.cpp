#include<iostream>

using namespace std;

main()
{
    int siz;
    cout<<"Enter the size of Queue: ";
    cin>>siz;
    int queue1[siz];
    int start = -1;  //starting pointer 'front' of queue
    int last = -1;   // end pointer 'end' of the queue

    //Various Queue operations
    // for input
    if(last==siz-1)
    {
        cout<<"No more space\n";
        exit(0);
    }
    else if()

}
